import { Typography } from '@mui/material';

const WriteClubPage = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to the Write Club</Typography>
    </div>
  );
};

export default WriteClubPage;
