// path: /mentorship/faqs

// import { Button } from "@mui/material";

// import { Feedback } from '@mui/icons-material';
// import { Typography } from '@mui/material';
// import FeedbackCard from 'componets'

const FeedbackSection = () => {
  //   const { title, feedbacks } = feedbackSecion;
  return (
    <div></div>
    // <div>
    //   <Typography variant="h4">Welcome to the Feedback section</Typography>
    //   <h2>{title}</h2>
    //   <div>
    //     {feedbacks.length > 0 ? (
    //       feedbacks.map((feedback, index) => <FeedbackCard />)
    //     ) : (
    //       <p></p>
    //     )}
    //   </div>
    // <Button></Button>
    // </div>
  );
};

export default FeedbackSection;
