import { Typography } from "@mui/material";

const ProgrammesPage = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to the Programmes Page</Typography>
    </div>
  );
};

export default ProgrammesPage;
