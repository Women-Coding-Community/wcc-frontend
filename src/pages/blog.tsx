import { Typography } from '@mui/material';

const BlogPage = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to the Blog Page</Typography>
    </div>
  );
};

export default BlogPage;
